<Window x:Class="BeEfficient.Pomodoro.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Be efficient! Pomodoro" Height="150" Width="220" ResizeMode="NoResize" WindowStyle="SingleBorderWindow" WindowStartupLocation="CenterScreen" >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="{Binding Progress}" FontSize="30" Foreground="SlateGray" HorizontalAlignment="Center" VerticalAlignment="Center" />
        <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center" Orientation="Horizontal">
            <Button Margin="5" Content="Start" Width="70" Command="{Binding StartCommand}" />
            <Button Margin="5" Content="Stop" Width="70" Command="{Binding StopCommand}" />
        </StackPanel>
    </Grid>
</Window>
